name: 🕒 Auto Facebook (Reels/Post) Dispatcher

on:
  schedule:
    # Chạy phút 17 và 47 mỗi giờ. Dispatcher tự lọc theo slot; nếu Reels và
    # FB Post cùng giờ, chúng lần lượt nhận hai lượt để không đụng session.
    - cron: '17,47 * * * *'
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'

      - name: Install dependencies
        run: |
          cd "Tool-Thread"/tele_bot
          npm install @supabase/supabase-js axios dotenv

      - name: Run Dispatcher
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          GITHUB_TOKEN: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}
          GITHUB_REPO: ${{ github.repository }}
        run: |
          cd "Tool-Thread"/tele_bot
          node cron_facebook_dispatcher.js
